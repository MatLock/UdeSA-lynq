{{- if .Values.manageSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: lynq-feeders-secret
  namespace: lynq-{{ .Values.k8s_namespace }}-namespace
  labels:
    app: lynq-feeders
type: Opaque
stringData:
  LYNQ_INTERNAL_TOKEN: {{ .Values.credentials.internal.token | quote }}
{{- end }}
