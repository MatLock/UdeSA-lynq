apiVersion: v1
kind: Service
metadata:
  name: lynq-iam-service
  namespace: lynq-{{ .Values.k8s_namespace }}-namespace
  labels:
    app: lynq-iam
spec:
  type: ClusterIP
  selector:
    app: lynq-iam
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: management
      port: 8081
      targetPort: management
