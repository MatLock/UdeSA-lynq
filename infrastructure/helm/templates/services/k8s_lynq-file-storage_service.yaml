apiVersion: v1
kind: Service
metadata:
  name: lynq-file-storage-service
  namespace: lynq-{{ .Values.k8s_namespace }}-namespace
  labels:
    app: lynq-file-storage
spec:
  type: ClusterIP
  selector:
    app: lynq-file-storage
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: management
      port: 8081
      targetPort: management
